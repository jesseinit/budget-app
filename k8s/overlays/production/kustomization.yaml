apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: budget-app

# Use base configurations as-is for production
resources:
  - ../../base

# Production uses GHCR images (no changes needed, base already configured)
